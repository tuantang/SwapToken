const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("ERC20 Token", function () {
  it("Initial ERC 20 Token", async () => {
    // write code for test smart contract in here
  });
});
